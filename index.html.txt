<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>DID | NVO987 | Nicholas Van-Orton</title>
  <meta name="description" content="NVO987 — Modern and Contemporary Visual Culture. Technical maintenance in progress until October 1, 2026.">
  <meta name="theme-color" content="#171918">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    /* =========================================================
       NVO987 — PREMIUM EDITORIAL DESIGN
       Complete CSS replacement
       ========================================================= */

    :root {
      --bg: #f2f0ea;
      --bg-soft: #e9e7e0;
      --ink: #171918;
      --ink-soft: #4e514d;
      --muted: #85877f;
      --line: rgba(23, 25, 24, 0.14);
      --accent: #a18b62;
      --accent-light: #c8b895;
      --white: #faf9f5;

      --serif: "Playfair Display", Georgia, serif;
      --sans: "Inter", Arial, sans-serif;

      --container: 1440px;
      --radius: 2px;

      --ease: cubic-bezier(.22, .61, .36, 1);
    }

    /* ---------- Reset ---------- */

    *,
    *::before,
    *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      min-height: 100vh;
      background:
        radial-gradient(
          circle at 85% 15%,
          rgba(161, 139, 98, 0.07),
          transparent 30%
        ),
        linear-gradient(
          135deg,
          var(--bg) 0%,
          #f5f3ee 48%,
          var(--bg-soft) 100%
        );
      color: var(--ink);
      font-family: var(--sans);
      font-weight: 400;
      -webkit-font-smoothing: antialiased;
      text-rendering: optimizeLegibility;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    button,
    input,
    textarea,
    select {
      font: inherit;
    }

    ::selection {
      background: var(--ink);
      color: var(--white);
    }

    /* ---------- Main Shell ---------- */

    body {
      display: flex;
      flex-direction: column;
    }

    main {
      flex: 1;
    }

    .container {
      width: min(
        calc(100% - 40px),
        var(--container)
      );
      margin-inline: auto;
    }

    /* ---------- Header ---------- */

    header {
      position: relative;
      z-index: 10;
      padding: 26px 0;
    }

    header::after {
      content: "";
      display: block;
      width: min(
        calc(100% - 40px),
        var(--container)
      );
      height: 1px;
      margin: 26px auto 0;
      background: var(--line);
    }

    .header-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 30px;
    }

    .brand {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 0.24em;
      text-transform: uppercase;
    }

    .brand-mark {
      position: relative;
      width: 28px;
      height: 28px;
      border: 1px solid var(--ink);
      transform: rotate(45deg);
      transition:
        transform .6s var(--ease),
        border-color .4s ease;
    }

    .brand-mark::after {
      content: "";
      position: absolute;
      inset: 6px;
      border: 1px solid var(--accent);
    }

    .brand:hover .brand-mark {
      transform: rotate(135deg);
      border-color: var(--accent);
    }

    .header-label {
      color: var(--muted);
      font-size: 10px;
      font-weight: 500;
      letter-spacing: 0.18em;
      text-transform: uppercase;
    }

    /* ---------- Hero ---------- */

    .hero {
      position: relative;
      display: grid;
      align-items: center;
      min-height: calc(100vh - 180px);
      padding: 80px 0 110px;
      overflow: hidden;
    }

    .hero::before {
      content: "NVO987";
      position: absolute;
      right: -55px;
      bottom: 10%;
      color: rgba(23, 25, 24, 0.025);
      font-family: var(--sans);
      font-size: clamp(100px, 18vw, 300px);
      font-weight: 600;
      letter-spacing: -0.08em;
      line-height: 0.8;
      pointer-events: none;
      user-select: none;
    }

    .hero-grid {
      position: relative;
      z-index: 1;
      display: grid;
      grid-template-columns: 1fr;
      gap: 70px;
    }

    .hero-meta {
      display: flex;
      align-items: center;
      gap: 18px;
      color: var(--muted);
      font-size: 10px;
      font-weight: 500;
      letter-spacing: 0.22em;
      line-height: 1.6;
      text-transform: uppercase;
    }

    .hero-meta::before {
      content: "";
      width: 46px;
      height: 1px;
      background: var(--accent);
    }

    .hero-content {
      max-width: 1100px;
    }

    .hero-title {
      max-width: 1050px;
      font-family: var(--serif);
      font-size: clamp(48px, 8vw, 116px);
      font-weight: 400;
      letter-spacing: -0.045em;
      line-height: 0.98;
    }

    .hero-title span {
      display: block;
    }

    .hero-title .accent {
      color: var(--accent);
      font-style: italic;
    }

    .hero-description {
      max-width: 570px;
      margin-top: 48px;
      color: var(--ink-soft);
      font-size: 14px;
      font-weight: 300;
      letter-spacing: 0.01em;
      line-height: 1.9;
    }

    /* ---------- Status ---------- */

    .status {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 20px;
      margin-top: 70px;
      padding-top: 22px;
      border-top: 1px solid var(--line);
    }

    .status-label {
      display: flex;
      align-items: center;
      gap: 10px;
      color: var(--muted);
      font-size: 9px;
      font-weight: 600;
      letter-spacing: 0.22em;
      text-transform: uppercase;
    }

    .status-dot {
      width: 7px;
      height: 7px;
      border-radius: 50%;
      background: var(--accent);
      box-shadow:
        0 0 0 5px rgba(161, 139, 98, 0.11);
      animation: pulse 2.8s ease-in-out infinite;
    }

    .status-text {
      color: var(--ink);
      font-family: var(--serif);
      font-size: clamp(20px, 2.2vw, 30px);
      font-weight: 400;
      line-height: 1.4;
    }

    @keyframes pulse {
      0%,
      100% {
        opacity: .65;
        transform: scale(.92);
      }

      50% {
        opacity: 1;
        transform: scale(1);
      }
    }

    /* ---------- Scroll Indicator ---------- */

    .scroll-indicator {
      position: absolute;
      bottom: 34px;
      left: 50%;
      display: none;
      flex-direction: column;
      align-items: center;
      gap: 12px;
      transform: translateX(-50%);
      color: var(--muted);
      font-size: 8px;
      font-weight: 600;
      letter-spacing: .25em;
      text-transform: uppercase;
    }

    .scroll-line {
      width: 1px;
      height: 46px;
      background: linear-gradient(
        to bottom,
        var(--accent),
        transparent
      );
    }

    /* ---------- Footer ---------- */

    footer {
      position: relative;
      background: var(--ink);
      color: var(--white);
      overflow: hidden;
    }

    footer::before {
      content: "";
      position: absolute;
      top: 0;
      right: 0;
      width: 42%;
      height: 1px;
      background: var(--accent);
    }

    footer::after {
      content: "";
      position: absolute;
      right: -100px;
      bottom: -160px;
      width: 420px;
      height: 420px;
      border: 1px solid rgba(200, 184, 149, 0.1);
      border-radius: 50%;
      pointer-events: none;
    }

    .footer-inner {
      position: relative;
      z-index: 1;
      display: grid;
      grid-template-columns: 1fr;
      gap: 55px;
      padding: 65px 0 30px;
    }

    .footer-brand {
      max-width: 560px;
    }

    .footer-brand h2 {
      font-family: var(--serif);
      font-size: clamp(42px, 7vw, 82px);
      font-weight: 400;
      letter-spacing: -0.045em;
      line-height: .95;
    }

    .footer-subtitle {
      margin-top: 22px;
      color: var(--accent-light);
      font-size: 11px;
      font-weight: 500;
      letter-spacing: .16em;
      line-height: 1.7;
      text-transform: uppercase;
    }

    .footer-name {
      margin-top: 9px;
      color: rgba(250, 249, 245, .58);
      font-family: var(--serif);
      font-size: 17px;
      font-style: italic;
    }

    .footer-contact {
      display: grid;
      gap: 18px;
    }

    .contact-label {
      margin-bottom: 4px;
      color: rgba(250, 249, 245, .4);
      font-size: 9px;
      font-weight: 600;
      letter-spacing: .2em;
      text-transform: uppercase;
    }

    .contact-link {
      position: relative;
      width: fit-content;
      color: var(--white);
      font-size: 13px;
      font-weight: 400;
      letter-spacing: .015em;
      transition: color .3s ease;
    }

    .contact-link::after {
      content: "";
      position: absolute;
      right: 0;
      bottom: -6px;
      left: 0;
      height: 1px;
      background: var(--accent);
      transform: scaleX(0);
      transform-origin: right;
      transition: transform .5s var(--ease);
    }

    .contact-link:hover {
      color: var(--accent-light);
    }

    .contact-link:hover::after {
      transform: scaleX(1);
      transform-origin: left;
    }

    .footer-bottom {
      display: flex;
      flex-direction: column;
      gap: 18px;
      padding: 25px 0;
      border-top: 1px solid rgba(250, 249, 245, .12);
      color: rgba(250, 249, 245, .4);
      font-size: 9px;
      font-weight: 500;
      letter-spacing: .16em;
      line-height: 1.7;
      text-transform: uppercase;
    }

    .footer-website {
      transition: color .3s ease;
    }

    .footer-website:hover {
      color: var(--accent-light);
    }

    /* ---------- Tablet ---------- */

    @media (min-width: 700px) {

      .container {
        width: min(
          calc(100% - 80px),
          var(--container)
        );
      }

      header {
        padding: 34px 0;
      }

      header::after {
        width: min(
          calc(100% - 80px),
          var(--container)
        );
        margin-top: 34px;
      }

      .hero {
        padding: 100px 0 130px;
      }

      .hero-grid {
        grid-template-columns: 100px 1fr;
        gap: 35px;
      }

      .hero-meta {
        align-self: start;
        margin-top: 14px;
        writing-mode: vertical-rl;
        transform: rotate(180deg);
      }

      .hero-meta::before {
        width: 1px;
        height: 42px;
      }

      .status {
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
        gap: 30px;
      }

      .footer-inner {
        grid-template-columns: 1.4fr .8fr;
        align-items: end;
        gap: 80px;
        padding: 85px 0 40px;
      }

      .footer-contact {
        justify-self: end;
        min-width: 280px;
      }

      .footer-bottom {
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
      }
    }

    /* ---------- Desktop ---------- */

    @media (min-width: 1100px) {

      .hero {
        min-height: calc(100vh - 170px);
        padding: 120px 0 150px;
      }

      .hero-grid {
        grid-template-columns: 130px 1fr;
        gap: 55px;
      }

      .hero-description {
        margin-top: 60px;
      }

      .status {
        max-width: 950px;
        margin-top: 95px;
      }

      .scroll-indicator {
        display: flex;
      }

      .footer-inner {
        padding-top: 105px;
      }
    }

    /* ---------- Large Screens ---------- */

    @media (min-width: 1600px) {

      .hero {
        padding-top: 150px;
      }

      .hero-grid {
        grid-template-columns: 170px 1fr;
      }

      .hero-title {
        font-size: 122px;
      }
    }

    /* ---------- Reduced Motion ---------- */

    @media (prefers-reduced-motion: reduce) {

      html {
        scroll-behavior: auto;
      }

      *,
      *::before,
      *::after {
        animation-duration: .01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: .01ms !important;
      }
    }

    /* ---------- Small Mobile ---------- */

    @media (max-width: 399px) {

      .container {
        width: min(
          calc(100% - 28px),
          var(--container)
        );
      }

      header::after {
        width: calc(100% - 28px);
      }

      .header-label {
        display: none;
      }

      .hero {
        padding-top: 65px;
      }

      .hero-title {
        font-size: 45px;
      }

      .hero-description {
        margin-top: 34px;
        font-size: 13px;
      }

      .status {
        margin-top: 55px;
      }

      .status-text {
        font-size: 19px;
      }

      .footer-inner {
        padding-top: 55px;
      }
    }
  </style>
</head>

<body>

  <!-- =========================================================
       HEADER
       ========================================================= -->

  <header>
    <div class="container header-inner">

      <a
        class="brand"
        href="https://identity.nvo987.us"
        aria-label="NVO987"
      >
        <span class="brand-mark" aria-hidden="true"></span>
        <span>NVO987</span>
      </a>

      <span class="header-label">
        Modern Visual Culture
      </span>

    </div>
  </header>


  <!-- =========================================================
       MAIN
       ========================================================= -->

  <main>

    <section class="hero">

      <div class="container hero-grid">

        <div class="hero-meta">
          Identity / 987
        </div>

        <div class="hero-content">

          <h1 class="hero-title">
            <span>DID</span>
            <span>NVO987</span>
            <span class="accent">Nicholas Van-Orton</span>
          </h1>

          <p class="hero-description">
            A contemporary platform dedicated to modern and
            contemporary visual culture, identity, research,
            and intellectual exploration.
          </p>

          <div class="status">

            <div class="status-label">
              <span class="status-dot"></span>
              <span>System Notice</span>
            </div>

            <p class="status-text">
              Technical maintenance in progress until
              October 1, 2026.
            </p>

          </div>

        </div>

      </div>

      <div class="scroll-indicator" aria-hidden="true">
        <span>Explore</span>
        <span class="scroll-line"></span>
      </div>

    </section>

  </main>


  <!-- =========================================================
       FOOTER
       ========================================================= -->

  <footer>

    <div class="container">

      <div class="footer-inner">

        <div class="footer-brand">

          <h2>NVO987</h2>

          <p class="footer-subtitle">
            Culture Visuelle Moderne et Contemporaine
          </p>

          <p class="footer-name">
            Nicholas Van-Orton
          </p>

        </div>


        <div class="footer-contact">

          <div>
            <p class="contact-label">Correspondence</p>
          </div>

          <a
            class="contact-link"
            href="mailto:nvo@nvo987.us"
          >
            nvo@nvo987.us
          </a>

          <a
            class="contact-link"
            href="mailto:contact@nexarium.us"
          >
            contact@nexarium.us
          </a>

          <a
            class="contact-link footer-website"
            href="https://www.nvo987.fr"
            target="_blank"
            rel="noopener noreferrer"
          >
            www.nvo987.fr
          </a>

        </div>

      </div>


      <div class="footer-bottom">

        <span>
          NVO987 — Modern &amp; Contemporary Visual Culture
        </span>

        <span>
          © 2026 Nicholas Van-Orton
        </span>

      </div>

    </div>

  </footer>

</body>
  </html> 
